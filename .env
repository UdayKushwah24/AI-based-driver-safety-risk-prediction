# ── Server ──
HOST=0.0.0.0
PORT=8000

# ── Drowsiness Detection ──
EYE_AR_THRESH=0.25
EYE_AR_CONSEC_FRAMES=20
YAWN_THRESH=25

# ── Risk Engine Weights (must sum to 1.0) ──
DROWSINESS_WEIGHT=0.6
FOG_WEIGHT=0.4

# ── Logging ──
LOG_LEVEL=INFO
